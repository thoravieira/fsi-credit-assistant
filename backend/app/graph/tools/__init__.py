"""SDD 06 §3 — tools for the negotiation deep agent.

`scenario.py` holds the two tools the main agent carries itself; `research.py`
holds the one tool each subagent carries. `case.py` is the runtime context all
of them read.
"""
