"""SDD 06 — the reasoning layer.

`negotiation.py` is the only place in the codebase that uses Deep Agents;
`subagents.py` declares the two research subagents it delegates to; and
`proposal.py` holds the pure functions that turn a negotiation turn into
something a human can approve.
"""
